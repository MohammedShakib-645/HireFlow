# models/__init__.py
# All ORM models are defined in app/models.py (SQLite-compatible).
# The models/ subdirectory contains PostgreSQL+pgvector production models (used with Docker/Postgres).
# This init is intentionally empty to avoid import conflicts in local dev mode.
